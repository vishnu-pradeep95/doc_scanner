<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white"
    app:rippleColor="@color/ripple_color">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_sm">

        <!-- Document thumbnail — loaded via Coil; falls back to cartoon document icon -->
        <ImageView
            android:id="@+id/imageDocumentThumbnail"
            android:layout_width="56dp"
            android:layout_height="72dp"
            android:scaleType="centerCrop"
            android:contentDescription="@null"
            android:background="@color/cartoon_surface_variant" />

        <!-- Document info -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/spacing_sm"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Document name - VISIBLE with explicit dark color -->
            <TextView
                android:id="@+id/textDocumentName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:fontFamily="@font/nunito_bold"
                android:textColor="?attr/colorOnSurface"
                android:textAppearance="@style/TextAppearance.Cartoon.TitleMedium"
                tools:text="Business Contract.pdf" />

            <!-- Document details (pages, size, date) -->
            <TextView
                android:id="@+id/textDocumentDetails"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:fontFamily="@font/nunito_regular"
                android:textAppearance="@style/TextAppearance.Cartoon.BodySmall"
                tools:text="5 pages • 1.2 MB • Dec 26, 2025" />

        </LinearLayout>

        <!-- Edit button with pencil icon -->
        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/content_desc_edit_pdf"
            android:padding="@dimen/spacing_xs"
            android:src="@drawable/ic_edit_small_mascot" />

        <!-- Share button with mascot icon -->
        <ImageButton
            android:id="@+id/btnShare"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/share_pdf"
            android:padding="@dimen/spacing_xs"
            android:src="@drawable/ic_share_small_mascot" />

        <!-- Delete button with mascot icon -->
        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/delete_document"
            android:padding="@dimen/spacing_xs"
            android:src="@drawable/ic_delete_small_mascot" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
