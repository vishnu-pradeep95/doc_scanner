<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/spacing_sm"
    android:layout_marginVertical="@dimen/spacing_xxs"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    android:clickable="true"
    android:focusable="true"
    app:rippleColor="@color/ripple_color">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- PDF Preview/Icon area with rounded top corners -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="@color/surface_variant"
            android:clipToOutline="true">

            <ImageView
                android:id="@+id/imagePreview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="@string/content_desc_document_preview"
                tools:src="@tools:sample/backgrounds/scenic" />

            <!-- PDF icon badge with cartoon theme -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="@dimen/spacing_xs"
                app:cardCornerRadius="18dp"
                app:cardElevation="3dp"
                app:cardBackgroundColor="?attr/colorPrimary">

                <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:src="@drawable/ic_pdf"
                    android:padding="6dp"
                    android:contentDescription="@string/content_desc_pdf_badge"
                    app:tint="?attr/colorOnPrimary" />

            </com.google.android.material.card.MaterialCardView>

        </FrameLayout>

        <!-- Document info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/spacing_sm">

            <TextView
                android:id="@+id/textDocName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:accessibilityDataSensitive="yes"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="?attr/colorOnSurface"
                android:textAppearance="@style/TextAppearance.Cartoon.TitleSmall"
                tools:ignore="UnusedAttribute"
                tools:text="Scan_20241227" />

            <TextView
                android:id="@+id/textDocDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xxs"
                android:textAppearance="@style/TextAppearance.Cartoon.BodySmall"
                tools:text="Dec 27, 2024" />

            <TextView
                android:id="@+id/textDocPages"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textAppearance="@style/TextAppearance.Cartoon.LabelSmall"
                tools:text="3 pages" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
