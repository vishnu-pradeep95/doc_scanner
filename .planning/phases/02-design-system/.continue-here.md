---
phase: 02-design-system
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-03-01T03:14:23.284Z
---

<current_state>
Phase 2: Design System planning is in progress. The phase directory was created, and a researcher agent was spawned but returned without creating the RESEARCH.md file (likely hit a context/rate limit mid-run). Need to re-run the researcher before planning can proceed.

Phase 1: Stability is COMPLETE — all 4 plans executed, all BUG-01 through BUG-08 fixed and verified.
</current_state>

<completed_work>

## Phase 1: Stability — COMPLETE (all committed)

- 01-01: ScannerViewModel refactored — immutable collections + SavedStateHandle (BUG-04, BUG-05)
- 01-02: Resource leaks fixed — use{} blocks for PdfUtils/PdfAnnotationRenderer/NativePdfView, temp file cleanup, undo/redo buttons removed (BUG-02, BUG-03, BUG-07)
- 01-03: Fragment crash safety (high-risk: PagesFragment, HistoryFragment, HomeFragment) + EXIF orientation fix + ImageUtils utility (BUG-01, BUG-08)
- 01-04: Fragment crash safety (remaining: PdfEditorFragment, PreviewFragment, CameraFragment, PdfViewerFragment) + bitmap OOM caps in ImageProcessor (BUG-01, BUG-06)
- Phase 1 verification: PASSED (5/5 must-haves)

## Phase 2: Design System — NOT YET STARTED

- Phase directory created: .planning/phases/02-design-system/
- Researcher agent spawned but RESEARCH.md was NOT created (agent returned with just agentId, no output)
</completed_work>

<remaining_work>

## Phase 2: Design System (DSYS-01 through DSYS-07)

Everything in Phase 2 is still to do:
1. Re-run researcher to create 02-RESEARCH.md (researcher failed without output)
2. Run gsd-planner to create PLAN.md files
3. Run gsd-plan-checker to verify plans
4. Execute plans
5. Verify phase goal

**Phase 2 requirements:**
- DSYS-01: Material type scale in styles.xml (Nunito headings/body/caption) across all screens
- DSYS-02: 8dp spacing grid in dimens.xml across all layouts
- DSYS-03: Coil library for all image/thumbnail loading (replaces manual bitmap loading)
- DSYS-04: Replace all 70 Toast calls with Snackbar/inline feedback
- DSYS-05: Move hardcoded contentDescription strings to strings.xml
- DSYS-06: Remove emoji from programmatic strings
- DSYS-07: Dark mode verified on all 7 screens

**Phases 3, 4, 5** — not yet started (Performance & Polish, Test Coverage, Release Readiness)
</remaining_work>

<decisions_made>

- Project scope: Polish + test only — no new features (confirmed by user)
- Depth: Comprehensive (8-12 phases recommended, we have 5)
- Mode: YOLO (auto-approve, parallel execution)
- Model profile: Quality (Opus for research/roadmap)
- Testing: Both manual + automated
- Phase ordering: Stability → Design System → Performance → Testing → Release (dependency-validated)
- Tests come AFTER fixes (Phase 4) to avoid testing broken behavior
- Undo/redo: Removed buttons entirely rather than implementing (simpler, safer for stability phase)
- Coil vs Glide: Coil (Kotlin-first) — confirmed in research
- Screenshot testing (Roborazzi/Paparazzi): Deferred to v2 (LOW confidence on tooling)
</decisions_made>

<blockers>

- No JDK in WSL2 environment — `./gradlew assembleDebug` cannot run. All verification is static code analysis. Test on a real device before marking phases unblocked.
- Phase 2 researcher failed without creating output — needs re-run on resume
</blockers>

<context>
We're doing a systematic quality pass on this Android document scanner app to reach portfolio/Play Store quality. The app is feature-complete (Phase 10 in its own roadmap) but needs stability fixes, design system consistency, performance polish, automated tests, and release readiness work.

Phase 1 is done — the app should no longer crash on fragment detach during long-running ops, should handle OOM on large documents, and should recover from process death.

Phase 2 is about making the UI feel intentional: defining the type scale and spacing grid, adding Coil for image loading (which also fixes memory/performance), replacing the 70 Toast calls with proper Snackbar feedback, moving strings to resources, and verifying dark mode.

The research for Phase 2 should be relatively straightforward — Material 3 type scale patterns, Coil 2.x integration, Snackbar best practices are all well-documented.
</context>

<next_action>
Start with: Run the Phase 2 researcher agent to create 02-RESEARCH.md. Use /gsd:plan-phase 2 which will detect that research is missing and re-run it automatically. The researcher prompt is in the conversation history — it needs to audit the existing source files for Toast calls, hardcoded strings, image loading patterns, and current type/spacing usage.
</next_action>
