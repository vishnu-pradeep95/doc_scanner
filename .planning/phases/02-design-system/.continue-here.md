---
phase: 02-design-system
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-03-01T14:58:51.786Z
---

<current_state>
Phase 2: Design System is CODE COMPLETE (all 5 plans executed, SUMMARYs written, ROADMAP updated).
However, verifier found 2 gaps (DSYS-05 partial, DSYS-06 partial) — gap closure plans needed.
Build environment is now working in WSL. APK builds successfully.
Next step: /gsd:plan-phase 2 --gaps (then /clear first)
</current_state>

<completed_work>

## Phase 2: Design System — All plans executed

- 02-01: Manifest theme switch (Theme.PDFScanner.Cartoon), dimens.xml (8dp grid), Nunito TextAppearance applied to 9 layouts, dark mode textColor fixed (?attr/colorOnSurface*)
- 02-02: Coil integrated in PagesAdapter + HistoryAdapter (downgraded 3.4.0→2.7.0 for Kotlin 1.9 compat)
- 02-03: Toast→Snackbar migration — 68 calls replaced, Extensions.kt created
- 02-04: Strings/emoji extracted to strings.xml across 7 layout files
- 02-05: Dark mode checkpoint — code-verified, device test deferred

## Build fixes applied this session

- compileSdk 34→35 (required by Coil + androidx.core:core-ktx:1.16.0)
- OFL.txt moved from res/font/ to app/ (only .ttf/.xml/.otf allowed in font dir)
- gradlew Unix shell script created (was missing, only gradlew.bat existed)
- local.properties created pointing to WSL SDK at ~/android-sdk
- Coil 3.4.0→2.7.0 (Coil 3.x requires Kotlin 2.3+, project uses Kotlin 1.9.21)
- coil3.load → coil.load imports in PagesAdapter + HistoryAdapter
- Duplicate Snackbar import removed from PagesFragment.kt
- pageCount scope fix in PdfUtils.kt (was referenced outside PdfRenderer.use{} block)

BUILD SUCCESSFUL — app/build/outputs/apk/debug/app-debug.apk
</completed_work>

<remaining_work>

## Gap closure for Phase 2 verifier (gaps_found — 2 gaps)

Gap 1 — DSYS-05 partial: fragment_pdf_editor.xml has 13 hardcoded toolbar button labels
- "Select", "Sign", "Text", "Highlight", "Stamp", "Draw", "Shapes", "Check", "Erase", "Color", "Size", "Stroke"
- Need: 12 new string entries in strings.xml + @string/ replacement in layout

Gap 2 — DSYS-06 partial: emoji remains in 4 layout files deferred by 02-04
- fragment_pages.xml: "➕ Add More", "📄 Create PDF", "🗑️ Delete" on MaterialButtons
- dialog_signature.xml: 5 emoji instances
- dialog_success.xml: "✨ Awesome!"
- dialog_ocr_result.xml: "📤 Share"

## After gap closure
- Phase 3: Performance & Polish (navigation transitions, haptics, edge-to-edge, PDF progress)
</remaining_work>

<decisions_made>

- Coil: Downgraded 3.4.0→2.7.0 — Coil 3.x pulls kotlin-stdlib 2.3.10 (metadata 2.3.0) incompatible with Kotlin compiler 1.9.21. Coil 2.7.0 is stable and provides identical API for our usage.
- Build env: WSL uses Linux Android SDK at ~/android-sdk (not Windows SDK at /mnt/c/...) — Windows .exe build tools don't work as Linux binaries
- Dark mode: Code-verified only (textColor fixed to ?attr/colorOnSurface*) — device verification deferred until Android Studio is configured
- JAVA_HOME must be set explicitly: export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
</decisions_made>

<blockers>

- DSYS-07 dark mode: Physical device test still needed — APK builds now, install on device and check all 7 screens in dark mode
- Kotlin 1.9.21 is old: Will limit library versions going forward (e.g., Coil 3.x, KSP 2.x). Phase 4 or 5 should include a Kotlin upgrade to 2.x.
- Build env note: Add JAVA_HOME and ANDROID_HOME to ~/.bashrc to persist across sessions (currently must export manually each time)
</blockers>

<context>
Phase 2 is functionally complete. The gap closure is small — just 2 targeted string extraction tasks in 5 layout files. After that, verifier should pass and Phase 2 can be officially marked complete.

The build environment is now working in WSL with Linux Android SDK. The user had Android Studio issues opening WSL projects from Windows, so we set up a native WSL build pipeline instead.

For JAVA_HOME persistence, the user should add to ~/.bashrc:
  export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
  export ANDROID_HOME="$HOME/android-sdk"
  export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
</context>

<next_action>
Start with: /clear, then /gsd:plan-phase 2 --gaps to create gap closure plans for the 2 remaining DSYS items. Then execute and re-run verifier.
</next_action>
