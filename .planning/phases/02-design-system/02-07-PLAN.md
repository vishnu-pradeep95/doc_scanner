---
phase: 02-design-system
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/res/layout/fragment_pages.xml
  - app/src/main/res/layout/dialog_signature.xml
  - app/src/main/res/layout/dialog_success.xml
  - app/src/main/res/layout/dialog_ocr_result.xml
autonomous: true
requirements: [DSYS-06]
gap_closure: true

must_haves:
  truths:
    - "Zero emoji characters appear in android:text attributes across all layout XML files"
    - "fragment_pages.xml MaterialButton labels use @string/ references with no emoji"
    - "dialog_signature.xml uses @string/ references for all android:text and contentDescription attributes"
    - "dialog_success.xml Awesome button uses a @string/ reference with no emoji"
    - "dialog_ocr_result.xml Share and Copy buttons use @string/ references with no emoji"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "New emoji-free string entries: awesome, sig_undo, sig_insert_btn"
      contains: "awesome"
    - path: "app/src/main/res/layout/fragment_pages.xml"
      provides: "Emoji-free MaterialButton android:text attributes"
    - path: "app/src/main/res/layout/dialog_signature.xml"
      provides: "Emoji-free android:text and contentDescription attributes"
    - path: "app/src/main/res/layout/dialog_success.xml"
      provides: "Emoji-free button text"
    - path: "app/src/main/res/layout/dialog_ocr_result.xml"
      provides: "Emoji-free button text"
  key_links:
    - from: "fragment_pages.xml btnAddMore"
      to: "strings.xml add_more"
      via: "android:text=\"@string/add_more\""
      pattern: "android:text=\"@string/add_more\""
    - from: "dialog_success.xml btnOk"
      to: "strings.xml awesome"
      via: "android:text=\"@string/awesome\""
      pattern: "android:text=\"@string/awesome\""
---

<objective>
Remove all emoji from android:text attributes in four layout files by converting them to @string/ references pointing to emoji-free string values. Where string resources already exist in strings.xml (from prior plans), reuse them. Where they do not exist, add new entries.

Purpose: Close DSYS-06 gap — four layout files still contain emoji in android:text attributes. Plan 02-04 explicitly deferred these files. DSYS-06 requires emoji removal from all android:text attributes without exception.

Output: strings.xml gains 3 new entries; 4 layout files converted to use @string/ references with no emoji in displayed text.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-VERIFICATION.md
@app/src/main/res/values/strings.xml
@app/src/main/res/layout/fragment_pages.xml
@app/src/main/res/layout/dialog_signature.xml
@app/src/main/res/layout/dialog_success.xml
@app/src/main/res/layout/dialog_ocr_result.xml
</context>

<interfaces>
<!-- Key existing strings.xml entries the executor will reuse. No exploration needed. -->

Existing string resources to reuse (already in strings.xml, no emoji):
- `@string/add_more` = "Add More"
- `@string/create_pdf` = "Create PDF"
- `@string/delete_selected` = "Delete"
- `@string/sig_draw_title` = "Draw Your Signature"
- `@string/sig_saved_title` = "Saved Signatures"
- `@string/sig_no_saved` = "No saved signatures yet"
- `@string/sig_draw_new` = "Draw New Signature"
- `@string/sig_hint` = "Sign here"
- `@string/clear` = "Clear"
- `@string/undo` = "Undo"
- `@string/sig_save_for_later` = "Save this signature for later"
- `@string/sig_insert` = "Insert Signature"
- `@string/cancel` = "Cancel"
- `@string/content_desc_sig_mascot` = "Signature mascot"
- `@string/share` = "Share"
- `@string/copy_text` = "Copy"
- `@string/close` = "Close"

New string entries needed (not yet in strings.xml):
- `awesome` = "Awesome!"  (for dialog_success.xml "✨ Awesome!" button)

Note: dialog_signature.xml "↩️ Undo" maps to @string/undo. The "✓ Insert" uses a Unicode checkmark (U+2713), not an emoji — map to @string/sig_insert which already says "Insert Signature". The "Name (e.g., My Initials)" TextInputLayout hint is hardcoded but is an android:hint (not android:text) — leave it for the Phase 5 Lint cleanup scope unless it appears in a gap.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add new string entries to strings.xml</name>
  <files>app/src/main/res/values/strings.xml</files>
  <action>
Add the following new string entry to strings.xml. Place it in the "<!-- Dialogs -->" section near the existing `success` and `pdf_created_successfully` strings (around line 175), or at the end of the file before </resources> if that section is not convenient to locate. Add only entries that do not already exist.

```xml
<!-- Dialog: Success button -->
<string name="awesome">Awesome!</string>
```

Do NOT add strings for: add_more, create_pdf, delete_selected, sig_draw_title, sig_saved_title, sig_no_saved, sig_draw_new, sig_hint, clear, undo, sig_save_for_later, sig_insert, cancel, share, copy_text, close — all of these already exist in strings.xml.

Verify the new entry does not duplicate any existing string name before writing.
  </action>
  <verify>
    <automated>grep -n '"awesome"' /home/vishnu/projects/doc_scanner/app/src/main/res/values/strings.xml</automated>
  </verify>
  <done>grep returns one line with `<string name="awesome">Awesome!</string>`. No emoji in the value. No duplicate string names introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Replace emoji android:text in all 4 layout files</name>
  <files>
    app/src/main/res/layout/fragment_pages.xml,
    app/src/main/res/layout/dialog_signature.xml,
    app/src/main/res/layout/dialog_success.xml,
    app/src/main/res/layout/dialog_ocr_result.xml
  </files>
  <action>
Apply the following specific replacements to each file. Make ONLY the changes listed — do not alter surrounding attributes, indentation, or constraints.

**fragment_pages.xml — 4 replacements:**

1. btnAddMore MaterialButton:
   `android:text="➕ Add More"` → `android:text="@string/add_more"`

2. btnCreatePdf MaterialButton (in buttonsLayout):
   `android:text="📄 Create PDF"` → `android:text="@string/create_pdf"`

3. btnDeleteSelected MaterialButton (in selectionButtonsLayout):
   `android:text="🗑️ Delete"` → `android:text="@string/delete_selected"`

4. btnCreatePdfSelected MaterialButton (in selectionButtonsLayout):
   `android:text="📄 Create PDF"` → `android:text="@string/create_pdf"`

**dialog_signature.xml — full audit and replacement of all hardcoded android:text and android:contentDescription:**

5. Header ImageView contentDescription (currently hardcoded "Signature mascot"):
   `android:contentDescription="Signature mascot"` → `android:contentDescription="@string/content_desc_sig_mascot"`

6. tvTitle TextView (header):
   `android:text="✍️ Draw Your Signature"` → `android:text="@string/sig_draw_title"`

7. tvSavedLabel TextView:
   `android:text="Saved Signatures"` → `android:text="@string/sig_saved_title"`

8. tvEmptySignatures TextView:
   `android:text="No saved signatures yet ✨"` → `android:text="@string/sig_no_saved"`

9. tvDrawLabel TextView:
   `android:text="Draw New Signature"` → `android:text="@string/sig_draw_new"`

10. tvSignHere TextView (inside signature pad):
    `android:text="Sign here ✍️"` → `android:text="@string/sig_hint"`

11. btnClear MaterialButton:
    `android:text="🗑️ Clear"` → `android:text="@string/clear"`

12. btnUndo MaterialButton:
    `android:text="↩️ Undo"` → `android:text="@string/undo"`

13. cbSaveSignature MaterialCheckBox:
    `android:text="💾 Save for later"` → `android:text="@string/sig_save_for_later"`

14. btnCancel MaterialButton (bottom actions):
    `android:text="Cancel"` → `android:text="@string/cancel"`

15. btnInsert MaterialButton (bottom actions):
    `android:text="✓ Insert"` → `android:text="@string/sig_insert"`

Note on replacement 15: "✓" (U+2713 CHECK MARK) is a Unicode symbol, not an emoji, but it is still a hardcoded string. @string/sig_insert = "Insert Signature" which is clearer and correctly localization-ready.

**dialog_success.xml — 1 replacement:**

16. btnOk MaterialButton:
    `android:text="✨ Awesome!"` → `android:text="@string/awesome"`

**dialog_ocr_result.xml — 2 replacements:**

17. btnCopy MaterialButton:
    `android:text="📋 Copy"` → `android:text="@string/copy_text"`

18. btnShare MaterialButton:
    `android:text="📤 Share"` → `android:text="@string/share"`

The btnClose MaterialButton already uses `android:text="Done"` — this is a hardcoded string but it maps to an existing resource. Replace it as well:

19. btnClose MaterialButton:
    `android:text="Done"` → `android:text="@string/done"`

After all replacements, no android:text or android:contentDescription in any of these 4 files should contain emoji characters or bare English words.
  </action>
  <verify>
    <automated>grep -rn 'android:text="[^@]' /home/vishnu/projects/doc_scanner/app/src/main/res/layout/fragment_pages.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_signature.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_success.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_ocr_result.xml</automated>
  </verify>
  <done>grep returns zero lines. All android:text attributes in the 4 files use @string/ references. Emoji characters (➕, 📄, 🗑️, ✍️, ✨, 💾, 🗑️, ↩️, 📋, 📤) no longer appear in any android:text value in these files.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n '"awesome"' app/src/main/res/values/strings.xml` returns exactly 1 line
2. `grep -rn 'android:text="[^@]' app/src/main/res/layout/fragment_pages.xml app/src/main/res/layout/dialog_signature.xml app/src/main/res/layout/dialog_success.xml app/src/main/res/layout/dialog_ocr_result.xml` returns zero lines
3. `grep -rn 'android:contentDescription="[^@]' app/src/main/res/layout/dialog_signature.xml` returns zero lines (Signature mascot contentDescription now uses @string/)
4. `grep -c '➕\|📄\|🗑️\|✍️\|✨\|💾\|↩️\|📋\|📤' app/src/main/res/layout/fragment_pages.xml app/src/main/res/layout/dialog_signature.xml app/src/main/res/layout/dialog_success.xml app/src/main/res/layout/dialog_ocr_result.xml` returns 0 for each file
</verification>

<success_criteria>
- strings.xml has `<string name="awesome">Awesome!</string>` added (exactly 1 new entry)
- All 4 layout files use @string/ references for every android:text attribute
- No emoji characters remain in android:text values across these 4 files
- dialog_signature.xml contentDescription for signature mascot ImageView uses @string/content_desc_sig_mascot
- DSYS-06 requirement is fully satisfied: emoji removed from all android:text attributes in all layouts
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-07-SUMMARY.md` following the summary template.
</output>
