---
phase: 02-design-system
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/res/layout/dialog_stamp_picker.xml
  - app/src/main/res/layout/dialog_text_input.xml
  - app/src/main/res/layout/item_saved_signature.xml
  - app/src/main/res/layout/fragment_home.xml
autonomous: true
requirements: [DSYS-05, DSYS-06]
gap_closure: true

must_haves:
  truths:
    - "dialog_stamp_picker.xml has zero hardcoded android:text or android:contentDescription attributes"
    - "dialog_text_input.xml has zero hardcoded android:text or android:contentDescription attributes"
    - "item_saved_signature.xml contentDescription for delete button uses @string/ reference"
    - "fragment_home.xml contentDescription for logo ImageView uses @string/ reference"
    - "No emoji characters appear in android:text attributes across all layout XML files"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "New string entries: stamp_picker_title, content_desc_stamp_mascot, text_annotation_title, text_size_label, color_label, preview_label, text_insert, content_desc_text_mascot, content_desc_delete_signature, content_desc_app_logo"
      contains: "stamp_picker_title"
    - path: "app/src/main/res/layout/dialog_stamp_picker.xml"
      provides: "Emoji-free, @string/-only android:text and android:contentDescription attributes"
    - path: "app/src/main/res/layout/dialog_text_input.xml"
      provides: "Emoji-free, @string/-only android:text and android:contentDescription attributes; programmatic values converted to tools:text"
    - path: "app/src/main/res/layout/item_saved_signature.xml"
      provides: "@string/ reference for delete button contentDescription"
    - path: "app/src/main/res/layout/fragment_home.xml"
      provides: "@string/ reference for logo ImageView contentDescription"
  key_links:
    - from: "dialog_stamp_picker.xml tvTitle"
      to: "strings.xml stamp_picker_title"
      via: "android:text=\"@string/stamp_picker_title\""
      pattern: "android:text=\"@string/stamp_picker_title\""
    - from: "dialog_text_input.xml tvTitle"
      to: "strings.xml text_annotation_title"
      via: "android:text=\"@string/text_annotation_title\""
      pattern: "android:text=\"@string/text_annotation_title\""
    - from: "dialog_text_input.xml tvPreviewLabel"
      to: "strings.xml preview_label"
      via: "android:text=\"@string/preview_label\""
      pattern: "android:text=\"@string/preview_label\""
---

<objective>
Close the final DSYS-05 and DSYS-06 gaps by extracting all remaining hardcoded android:text and android:contentDescription attributes in four layout files to strings.xml, and removing all emoji from android:text values.

Purpose: Plans 02-06 and 02-07 closed the originally identified gaps but never scanned dialog_stamp_picker.xml or dialog_text_input.xml. These two production dialogs (inflated by StampPickerDialogFragment and TextInputDialogFragment, both reachable from PdfEditorFragment) still contain emoji in android:text and multiple hardcoded English strings. Two minor contentDescription gaps in item_saved_signature.xml and fragment_home.xml remain as well. This plan closes all four files, fully satisfying DSYS-05 and DSYS-06.

Output: strings.xml gains 10 new entries; 4 layout files converted to use @string/ references with no emoji in displayed text; tvSizeValue and tvPreview in dialog_text_input.xml converted to tools:text (following the tvPageInfo pattern from 02-06).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system/02-VERIFICATION.md
@app/src/main/res/values/strings.xml
@app/src/main/res/layout/dialog_stamp_picker.xml
@app/src/main/res/layout/dialog_text_input.xml
@app/src/main/res/layout/item_saved_signature.xml
@app/src/main/res/layout/fragment_home.xml
</context>

<interfaces>
<!-- Key existing strings.xml entries the executor will reuse. No exploration needed. -->

Existing string resources to REUSE (already in strings.xml, do NOT add again):
- `@string/cancel` = "Cancel"  (line 86) — used for btnCancel in both dialogs

New string entries NEEDED (not yet in strings.xml — add all 10):

For dialog_stamp_picker.xml:
- `stamp_picker_title` = "Select Stamp"         (replaces "👍 Select Stamp" — emoji removed)
- `content_desc_stamp_mascot` = "Stamp mascot"  (replaces hardcoded contentDescription)

For dialog_text_input.xml:
- `text_annotation_title` = "Add Text"          (replaces "✏️ Add Text" — emoji removed)
- `text_size_label` = "Text Size"               (replaces hardcoded android:text on tvSizeLabel)
- `color_label` = "Color"                       (replaces hardcoded android:text on tvColorLabel)
- `preview_label` = "Preview"                   (replaces "👁️ Preview" — emoji removed)
- `text_insert` = "Insert"                      (replaces "✓ Insert" — Unicode symbol removed)
- `content_desc_text_mascot` = "Text mascot"    (replaces hardcoded contentDescription)

For item_saved_signature.xml:
- `content_desc_delete_signature` = "Delete signature"  (replaces hardcoded contentDescription)

For fragment_home.xml:
- `content_desc_app_logo` = "App logo"          (replaces hardcoded contentDescription)

Programmatic values — use tools:text (NOT string resources, following tvPageInfo pattern from 02-06):
- dialog_text_input.xml tvSizeValue: android:text="24" → tools:text="24" (value set by TextInputDialogFragment at runtime)
- dialog_text_input.xml tvPreview: android:text="Sample Text" → tools:text="Sample Text" (value set by TextInputDialogFragment at runtime)

android:hint in dialog_text_input.xml — LEAVE AS-IS:
- android:hint="Enter your text" on tilText TextInputLayout — android:hint scope is deferred per established project decision (02-07: android:hint explicitly deferred to Phase 5 Lint cleanup). Do NOT change this attribute.

Place new string entries in strings.xml at the end of the file, before </resources>, in a new comment block:
```xml
<!-- Dialog: Stamp Picker -->
<string name="stamp_picker_title">Select Stamp</string>
<string name="content_desc_stamp_mascot">Stamp mascot</string>

<!-- Dialog: Text Annotation -->
<string name="text_annotation_title">Add Text</string>
<string name="text_size_label">Text Size</string>
<string name="color_label">Color</string>
<string name="preview_label">Preview</string>
<string name="text_insert">Insert</string>
<string name="content_desc_text_mascot">Text mascot</string>

<!-- Content descriptions: item_saved_signature + fragment_home -->
<string name="content_desc_delete_signature">Delete signature</string>
<string name="content_desc_app_logo">App logo</string>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 10 new string entries to strings.xml</name>
  <files>app/src/main/res/values/strings.xml</files>
  <action>
Append the following block to strings.xml immediately before the closing `</resources>` tag. Verify first that none of these string names already exist (grep for each name). Add only the entries listed below — do NOT add entries for `cancel`, which already exists at line 86.

```xml

    <!-- Dialog: Stamp Picker -->
    <string name="stamp_picker_title">Select Stamp</string>
    <string name="content_desc_stamp_mascot">Stamp mascot</string>

    <!-- Dialog: Text Annotation -->
    <string name="text_annotation_title">Add Text</string>
    <string name="text_size_label">Text Size</string>
    <string name="color_label">Color</string>
    <string name="preview_label">Preview</string>
    <string name="text_insert">Insert</string>
    <string name="content_desc_text_mascot">Text mascot</string>

    <!-- Content descriptions: item_saved_signature + fragment_home -->
    <string name="content_desc_delete_signature">Delete signature</string>
    <string name="content_desc_app_logo">App logo</string>
```

No emoji in any string value. No duplicate string names introduced.
  </action>
  <verify>
    <automated>grep -c 'name="stamp_picker_title"\|name="content_desc_stamp_mascot"\|name="text_annotation_title"\|name="text_size_label"\|name="color_label"\|name="preview_label"\|name="text_insert"\|name="content_desc_text_mascot"\|name="content_desc_delete_signature"\|name="content_desc_app_logo"' /home/vishnu/projects/doc_scanner/app/src/main/res/values/strings.xml</automated>
  </verify>
  <done>grep -c returns 10. All 10 new string names appear exactly once in strings.xml. No emoji in any of the 10 values.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded attributes in 4 layout files</name>
  <files>
    app/src/main/res/layout/dialog_stamp_picker.xml,
    app/src/main/res/layout/dialog_text_input.xml,
    app/src/main/res/layout/item_saved_signature.xml,
    app/src/main/res/layout/fragment_home.xml
  </files>
  <action>
Apply ONLY the specific replacements listed below to each file. Do not alter surrounding attributes, indentation, constraints, or any other content.

**dialog_stamp_picker.xml — 3 replacements:**

1. ImageView contentDescription (line 27):
   `android:contentDescription="Stamp mascot"` → `android:contentDescription="@string/content_desc_stamp_mascot"`

2. tvTitle TextView android:text (line 34):
   `android:text="👍 Select Stamp"` → `android:text="@string/stamp_picker_title"`

3. btnCancel MaterialButton android:text (line 72):
   `android:text="Cancel"` → `android:text="@string/cancel"`

After: dialog_stamp_picker.xml has zero hardcoded android:text or android:contentDescription attributes.

---

**dialog_text_input.xml — 9 replacements:**

4. Header ImageView contentDescription (line 27):
   `android:contentDescription="Text mascot"` → `android:contentDescription="@string/content_desc_text_mascot"`

5. tvTitle TextView android:text (line 34):
   `android:text="✏️ Add Text"` → `android:text="@string/text_annotation_title"`

6. tvSizeLabel TextView android:text (line 95):
   `android:text="Text Size"` → `android:text="@string/text_size_label"`

7. tvSizeValue TextView android:text (line 105):
   `android:text="24"` → `tools:text="24"`
   Also add xmlns:tools="http://schemas.android.com/tools" to the root ConstraintLayout if not already present.
   Rationale: value is overwritten at runtime by TextInputDialogFragment — this is a design-time placeholder only, following the tvPageInfo pattern from plan 02-06.

8. tvColorLabel TextView android:text (line 144):
   `android:text="Color"` → `android:text="@string/color_label"`

9. tvPreviewLabel TextView android:text (line 235):
   `android:text="👁️ Preview"` → `android:text="@string/preview_label"`

10. tvPreview TextView android:text (line 255):
    `android:text="Sample Text"` → `tools:text="Sample Text"`
    Rationale: value is overwritten at runtime — this is a design-time placeholder only.

11. btnCancel MaterialButton android:text (line 283):
    `android:text="Cancel"` → `android:text="@string/cancel"`

12. btnInsert MaterialButton android:text (line 291):
    `android:text="✓ Insert"` → `android:text="@string/text_insert"`

DO NOT touch android:hint="Enter your text" on tilText (line 59) — android:hint scope is explicitly deferred per project decision (02-07). Leave it unchanged.

After: dialog_text_input.xml has zero hardcoded android:text or android:contentDescription attributes (android:hint on tilText is excluded per scope decision).

---

**item_saved_signature.xml — 1 replacement:**

13. btnDelete ImageButton contentDescription (line 37):
    `android:contentDescription="Delete signature"` → `android:contentDescription="@string/content_desc_delete_signature"`

After: item_saved_signature.xml has zero hardcoded android:contentDescription attributes.

---

**fragment_home.xml — 1 replacement:**

14. Logo ImageView contentDescription (line 58):
    `android:contentDescription="App logo"` → `android:contentDescription="@string/content_desc_app_logo"`

After: fragment_home.xml logo ImageView uses @string/content_desc_app_logo.
  </action>
  <verify>
    <automated>grep -rn 'android:text="[^@]' /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_stamp_picker.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_text_input.xml &amp;&amp; grep -rn 'android:contentDescription="[^@]' /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_stamp_picker.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/dialog_text_input.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/item_saved_signature.xml /home/vishnu/projects/doc_scanner/app/src/main/res/layout/fragment_home.xml</automated>
  </verify>
  <done>
Both grep commands return zero matching lines.

Specifically:
- dialog_stamp_picker.xml: tvTitle uses @string/stamp_picker_title, btnCancel uses @string/cancel, ImageView uses @string/content_desc_stamp_mascot
- dialog_text_input.xml: tvTitle uses @string/text_annotation_title, tvSizeLabel uses @string/text_size_label, tvSizeValue uses tools:text="24", tvColorLabel uses @string/color_label, tvPreviewLabel uses @string/preview_label, tvPreview uses tools:text="Sample Text", btnCancel uses @string/cancel, btnInsert uses @string/text_insert, header ImageView uses @string/content_desc_text_mascot
- item_saved_signature.xml: btnDelete uses @string/content_desc_delete_signature
- fragment_home.xml: logo ImageView uses @string/content_desc_app_logo
- android:hint="Enter your text" on tilText is unchanged (deferred scope)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

1. All 10 new string names present:
   `grep -c 'name="stamp_picker_title"\|name="content_desc_stamp_mascot"\|name="text_annotation_title"\|name="text_size_label"\|name="color_label"\|name="preview_label"\|name="text_insert"\|name="content_desc_text_mascot"\|name="content_desc_delete_signature"\|name="content_desc_app_logo"' app/src/main/res/values/strings.xml`
   Expected: 10

2. Zero hardcoded android:text in stamp and text-input dialogs:
   `grep -rn 'android:text="[^@]' app/src/main/res/layout/dialog_stamp_picker.xml app/src/main/res/layout/dialog_text_input.xml`
   Expected: zero lines (tools:text lines do not match this pattern)

3. Zero hardcoded contentDescriptions across all 4 files:
   `grep -rn 'android:contentDescription="[^@]' app/src/main/res/layout/dialog_stamp_picker.xml app/src/main/res/layout/dialog_text_input.xml app/src/main/res/layout/item_saved_signature.xml app/src/main/res/layout/fragment_home.xml`
   Expected: zero lines

4. No emoji in any of the 4 layout files:
   `grep -Pn '[\x{1F300}-\x{1FFFF}]|\u270F|\u1F441|\u1F44D' app/src/main/res/layout/dialog_stamp_picker.xml app/src/main/res/layout/dialog_text_input.xml`
   Expected: zero lines

5. Broad layout scan — confirm no other hardcoded text remains in any layout:
   `grep -rn 'android:text="[^@{]' app/src/main/res/layout/`
   Expected: zero lines from the 4 target files; review any other files if lines appear
</verification>

<success_criteria>
- strings.xml has all 10 new entries (stamp_picker_title, content_desc_stamp_mascot, text_annotation_title, text_size_label, color_label, preview_label, text_insert, content_desc_text_mascot, content_desc_delete_signature, content_desc_app_logo)
- dialog_stamp_picker.xml: zero hardcoded android:text or android:contentDescription attributes
- dialog_text_input.xml: zero hardcoded android:text or android:contentDescription attributes; tvSizeValue and tvPreview use tools:text; android:hint="Enter your text" unchanged (deferred)
- item_saved_signature.xml: btnDelete contentDescription uses @string/content_desc_delete_signature
- fragment_home.xml: logo ImageView contentDescription uses @string/content_desc_app_logo
- DSYS-05 satisfied: no hardcoded English strings remain in any layout XML file (excluding android:hint per scope decision)
- DSYS-06 satisfied: no emoji characters remain in android:text attributes across all layout XML files
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-08-SUMMARY.md` following the summary template.
</output>
