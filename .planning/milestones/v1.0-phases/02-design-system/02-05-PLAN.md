---
phase: 02-design-system
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified: []
autonomous: false
requirements:
  - DSYS-07

must_haves:
  truths:
    - "All 7 screens render correctly in dark mode — text is readable, icons are visible"
    - "No screen shows white text on white background or black text on black background in dark mode"
    - "Snackbar messages are visible in dark mode"
    - "Page thumbnails and document list are readable in dark mode"
  artifacts: []
  key_links:
    - from: "values-night/themes.xml"
      to: "themes_cartoon.xml"
      via: "dark mode theme override"
      pattern: "Theme.PDFScanner.Cartoon"
---

<objective>
Verify dark mode renders correctly on all 7 app screens. This is a human checkpoint because dark mode visual correctness cannot be automated — it requires visual inspection on a device or emulator.

Purpose: The Phase 2 typography work (Plans 01-04) changed TextAppearance textColor from hardcoded colors to ?attr/colorOnSurface. This must be verified visually to confirm dark mode now works correctly after those changes.
Output: Confirmed pass or list of specific dark mode issues to fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-design-system/02-RESEARCH.md

<interfaces>
<!-- The 7 screens to verify -->
1. HomeFragment      — home screen with recent documents and quick actions
2. CameraFragment    — camera viewfinder with capture button
3. PreviewFragment   — filter/style controls and retake/add page buttons
4. PagesFragment     — page thumbnail grid with delete/rotate buttons
5. HistoryFragment   — document list with edit/share/delete actions
6. PdfViewerFragment — PDF page viewer with prev/next controls
7. SettingsFragment  — theme/filter settings with switches

<!-- Dark mode switch location -->
System Settings -> Display -> Dark theme (or Quick Settings toggle)
OR within the app: Settings screen -> Dark Mode toggle

<!-- Known dark mode risk areas from research -->
- BodyMedium and BodySmall TextAppearance (fixed in Plan 01 — verify fix worked)
- Snackbar text/background in dark mode (Material 3 handles this automatically)
- Dialog backgrounds (bg_dialog_rounded.xml hardcodes a light color — flag if visible)
- Bottom navigation active indicator color in dark mode
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 design system:
    - Plan 01: Cartoon theme activated, dimens.xml created, TextAppearance dark mode colors fixed
    - Plan 02: Coil integrated in PagesAdapter and HistoryAdapter
    - Plan 03: All 68 Toast calls replaced with Snackbar
    - Plan 04: Hardcoded strings and emoji moved to strings.xml
  </what-built>
  <how-to-verify>
    **Setup:**
    Build and install the debug APK, then enable dark mode via System Settings -> Display -> Dark theme.

    **Verify each screen in dark mode:**

    1. Home screen: Title "PDF Scanner" is readable, quick action buttons have visible labels, recent documents list (if any) shows readable text
    2. Camera screen: Open a new scan — viewfinder opens, capture button visible
    3. Preview screen: After capturing — filter labels ("Style", "Enhanced", "B&W") readable, buttons ("Retake", "Edit", "Add Page") have visible text
    4. Pages screen: Page thumbnails load with placeholder, page numbers readable, delete/rotate buttons visible
    5. History screen: Document names and details (pages, size, date) readable; if empty — empty state text readable
    6. PDF Viewer screen: "Page X of Y" label readable, previous/next arrows visible
    7. Settings screen: "Appearance", "Dark Mode", "Default Filter", "About" sections all readable

    **Verify Snackbar in dark mode:**
    Trigger any action that previously showed a Toast (e.g., tap "View All" in History if no docs, or attempt an operation that fails) — confirm Snackbar appears with readable text.

    **Check for regressions:**
    - Any screen with invisible text (white on white or black on black)
    - Any icon that is invisible against the dark background
    - Any card or dialog with wrong background color

    **Light mode sanity check:**
    Switch back to light mode and confirm the home screen still renders with Nunito font (not system sans-serif) — this verifies the manifest theme switch from Plan 01 worked.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all screens look correct in dark mode.
    Or describe specific issues (e.g., "HistoryFragment body text invisible" or "Snackbar not appearing") and Claude will create fix tasks.
  </resume-signal>
</task>

</tasks>

<verification>
Human visual verification on device/emulator. No automated check possible for visual dark mode correctness.
</verification>

<success_criteria>
- All 7 screens render without unreadable text in dark mode
- Snackbar visible and readable in dark mode
- No emoji visible in button labels or dialog text
- App uses Nunito font in light mode (cartoon theme confirmed active)
- User types "approved" to confirm Phase 2 complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system/02-05-SUMMARY.md` using the summary template.
</output>
